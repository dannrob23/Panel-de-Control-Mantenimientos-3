# Archivos locales / temporales
uploads/
__pycache__/
*.pyc
app_backup_*.py
*.log

# Respaldos y artefactos que no se despliegan
*.tmp
~$*

# Entorno virtual local (no subir dependencias)
.venv/
venv/

# Configuración local con secretos (modo_admin, modo_publico) — NO subir
.streamlit/secrets.toml
.gstack/
